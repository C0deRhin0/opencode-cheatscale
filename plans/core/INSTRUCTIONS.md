# Project-Specific Instructions (Context Layer)

This file contains the "Application Logic" and unique requirements for this project. These instructions build upon the standard rules in `<root_folder>/.opencode/instructions/INSTRUCTIONS.md`.

## Tier Architecture & Workspace
- **Implementation Layer**: All application source code MUST be contained within the **`<root_folder>/codebase/`** directory.
- **Context Layer**: Project roadmap, conventions, and research live in **`<root_folder>/plans/$SCOPE/*`**.
- **Root Directory**: Mandatory secondary outputs (`documentation.md`, `project.md`, daily reports) are maintained in the project root.

---

## Core Project Logic
- **Master Reference**: [roadmap.md] `<root_folder>/plans/$SCOPE/roadmap.md` is the primary source of truth for all technical phases.
- **Coding Conventions**: Strictly follow [coding_convention.md] `<root_folder>/plans/$SCOPE/coding_convention.md` for development framework coding convention.

---

## REQUIRED SECONDARY OUTPUTS

In addition to the project source code, the following outputs are **MANDATORY** and must be updated with every completed Phase (or Day, as requested in prompt):

### NONE SO FAR.