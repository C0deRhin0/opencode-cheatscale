interface:
  display_name: "CheatScale"
  short_description: "Repo-specific patterns and workflows for CheatScale"
  default_prompt: "Use the CheatScale repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
