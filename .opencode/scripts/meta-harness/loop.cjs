#!/usr/bin/env node

console.log('Autonomous meta-harness loops are disabled.');
console.log('Use /harness-optimize diagnose, review the local report, then apply approved changes manually.');
process.exit(1);
